// Shared prefilled mailto targets for the /picking page CTAs.
// The static site has no backend, so primary CTAs are mailto (see docs/design.md).
export const mailtoMain = `mailto:piotr.zaworski@teambear.io?subject=${encodeURIComponent(
  'Bezpłatna analiza procesu - obsługa zamówień',
)}`;
